
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.auroraos.app',
    appName: 'Aurora OS.js',
    webDir: 'dist',
    server: {
        androidScheme: 'https'
    }
};

export default config;
